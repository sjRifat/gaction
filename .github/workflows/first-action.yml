name: First Workflow # workflow name
on: push # which will trigger
jobs:
  first-job: # job name
    #the runner env
    runs-on: ubuntu-latest
    steps:
      - name: get location # step name
        run: pwd
        
